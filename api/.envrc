export DATABASE_NAME="band_repertory"
export DATABASE_USER="root"
export DATABASE_PASSWORD="041102"
export DATABASE_HOST="localhost"
export DATABASE_PORT="3306"
export DEBUG="True"
export ALLOWED_HOSTS="127.0.0.1,localhost"

# for container in msql
# do
#     CONTAINER_STARTED=$(docker container ls --filter name=$container -q)
#     if [ "$CONTAINER_STARTED" = "" ]; then
#         docker start $container
#     fi
# done
